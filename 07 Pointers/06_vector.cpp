#include <bits/stdc++.h>
using namespace std;

void solve(vector<int> arr)
{
    cout << sizeof(arr) << endl;
}

int main()
{
    vector<int> v;

    v.push_back(1);
    v.push_back(2);
    v.push_back(3);

    solve(v);

    return 0;
}