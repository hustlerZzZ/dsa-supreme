#include <bits/stdc++.h>
using namespace std;

int main()
{
    // creation
    string str;

    cout << "Enter the input : " << endl;

    // input
    // cin >> str;
    // OR
    getline(cin, str);

    // OR
    // str.push_back(' ');
    // str.push_back('P');
    // str.push_back('a');
    // str.push_back('l');

    // output
    cout << "Str : " << str << endl;
    cout << str[0] << endl;

    return 0;
}